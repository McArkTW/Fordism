writer        = console
writer.level  = info
writer.format = {date:yyyy-MM-dd HH:mm:ss} {level|min-size=5} {class-name}.{method}: {message}

# Jetty/Javalin route through slf4j-tinylog; keep their startup chatter at warn.
level@org.eclipse.jetty = warn
level@io.javalin        = info
